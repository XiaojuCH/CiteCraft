# Expected Outputs

This directory is the snapshot target for the P0 golden path.

`brief.json`, `brief.md`, `literature_matrix.json`, `literature_matrix.md`, `slides.json`, and `slides.md` are committed after generation.

HTML previews may be generated at runtime by the demo shell or written here by the pipeline runner.

